name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  packages: write

env:
  VERSION: ${{ github.ref_name }}

jobs:
  container:
    name: Publish container image
    runs-on: ubuntu-latest

    env:
      IMAGE: ghcr.io/${{ github.repository }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Login to ghcr.io
        run: |
          echo "${{ github.token }}" | podman login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build image
        run: |
          podman build --tag "${IMAGE}:${{ github.sha }}" .

      - name: Push image
        run: |
          for TAG in "latest" "${VERSION}"; do
            podman tag "${IMAGE}:${{ github.sha }}" "${IMAGE}:${TAG}"
            podman push "${IMAGE}:${TAG}"
          done

  helm:
    name: Publish helm chart
    runs-on: ubuntu-latest

    needs:
      - container

    env:
      REGISTRY: ghcr.io/${{ github.repository }}/charts

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Login to ghcr.io
        run: |
          echo "${{ github.token }}" | helm registry login "ghcr.io" -u "${{ github.actor }}" --password-stdin

      - name: Package chart
        run: |
          helm package "charts/ical-proxy" --version "${VERSION}" --app-version "${VERSION}"

      - name: Push chart
        run: |
          helm push "ical-proxy-${VERSION}.tgz" "oci://${REGISTRY}"
